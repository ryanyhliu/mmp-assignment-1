You can use these files to check the output matches the expected output for 4096 coordinates.

Compiler instruction: gcc compare.c dat_utils.c -o compare -std=c99

Usage: ./compare <filename_check> <filename_ref>
